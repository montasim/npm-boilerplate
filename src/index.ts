/**
 * Displays a welcome message to the console.
 *
 * This is the main exported function of the npm-boilerplate package.
 * It provides a simple greeting message that can be used as a starting
 * point for your own npm package.
 *
 * @example
 * ```ts
 * import welcome from 'npm-boilerplate';
 * welcome();
 * // Output: Welcome to the npm boilerplate.
 * ```
 *
 * @returns {void}
 */
const welcome = () => console.info(`Welcome to the npm boilerplate.`);

export default welcome;
